- defaults:
    link:
      relink: true

- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- link:

    ~/.bashrc: bash/bashrc
      

    ~/.vim: vim-files/vim
    ~/.vimrc: vim/vimrc
    ~/.vimperatorrc: vim/vimperatorrc

