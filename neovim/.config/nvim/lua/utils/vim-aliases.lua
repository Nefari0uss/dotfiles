local alias = {}

alias.cmd = vim.cmd; -- Command function
alias.api = vim.api; -- Neovim API
alias.lsp = vim.lsp; -- LSP API
alias.fn = vim.fn;   -- Vim function
alias.g = vim.g;     -- Vim globals
alias.opt = vim.opt; -- Vim optionals

return alias
