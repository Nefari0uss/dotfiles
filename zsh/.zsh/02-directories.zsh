#!/bin/zsh
# Nefari0uss
#
# Directory shortcuts.
# `hash -d <name>=<path>` makes ~<name> a shortcut for <path>.
# You can use this ~name anywhere you would specify a dir, not just with `cd`!

hash -d zsh=$ZDOTDIR
hash -d git="$HOME/.config/git"
hash -d vim="$HOME/.config/git"
hash -d nvim="$HOME/.config/nvim"
